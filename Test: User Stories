Test: User Stories

1. User starts the app
2. App starts showing user current location in the map. 
3. Map show nearby coffee shops
4. When user chooses a coffee shop, a tooltip pops up at selected shop. 
	a. A tooltip shows shop name, address, waiting time, rating
5. When user clicks on the tooltip:
	a. menu page of the selected shop shows up.
	b. menu page shows sell items
	c. each item has an image and price tag
6. User clicks on an item to select the desired item.

